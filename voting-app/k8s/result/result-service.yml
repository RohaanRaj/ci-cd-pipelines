apiVersion: v1
kind: Service
metadata:
  name: result-page-service
  namespace: voting-app
spec:
  type: NodePort
  selector:
    app: result
  ports:
    - name: UI
      port: 8081
      targetPort: 80

    - name: debug
      port: 9229
      targetPort: 9229
